package main

import (
	"fmt"
)

type ListNode struct {
	Val  int
	Next *ListNode
}

func reverseAry(list *ListNode) []int {

	return []int{}
}

func addTwoNumbers(l1 *ListNode, l2 *ListNode) *ListNode {
	reverseAry(l1)
	return &ListNode{}
}

func main() {
	m := make(map[int]string)
	m[0] = "a"
	var a = 2
	//a, _ = math.Pow(a, 2)
	fmt.Println(a)
	var slc []int
	slc = append(slc, 1)
	fmt.Println(slc, m)
	//addTwoNumbers(l1, l2)
}
